/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PagedModelPostViewDto } from '../../models/paged-model-post-view-dto';

export interface GetPostViews$Params {
  postId: number;

/**
 * Zero-based page index (0..N)
 */
  page?: number;

/**
 * The size of the page to be returned
 */
  size?: number;

/**
 * Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
 */
  sort?: Array<string>;
}

export function getPostViews(http: HttpClient, rootUrl: string, params: GetPostViews$Params, context?: HttpContext): Observable<StrictHttpResponse<PagedModelPostViewDto>> {
  const rb = new RequestBuilder(rootUrl, getPostViews.PATH, 'get');
  if (params) {
    rb.path('postId', params.postId, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
    rb.query('sort', params.sort, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PagedModelPostViewDto>;
    })
  );
}

getPostViews.PATH = '/api/posts/{postId}/views';
