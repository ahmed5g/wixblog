/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CommentResponse } from '../../models/comment-response';

export interface GetCommentReplies$Params {
  id: number;
}

export function getCommentReplies(http: HttpClient, rootUrl: string, params: GetCommentReplies$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<CommentResponse>>> {
  const rb = new RequestBuilder(rootUrl, getCommentReplies.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<CommentResponse>>;
    })
  );
}

getCommentReplies.PATH = '/api/v1/comments/{id}/replies';
